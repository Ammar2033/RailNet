# Contributing

Areas: compiler, runtime, models, verification, topology, hardware, docs, benchmarks.

Requirements per PR: tests, docs, honest benchmark (see SPEC.md).

Run: `pytest`, `ruff check`, `mypy railnet`.
