# STORY — RailNet research journey

RailNet-256 → topology exact → shared rails → shared multiplication → real Gemma BF16 → learned basis → repair → 4494/4494 exact → MLP 3/3 → global layer fabric → linear runtime → transformer block → KV cache → full forward → generation → PCIe architecture
